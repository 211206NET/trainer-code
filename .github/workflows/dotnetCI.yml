name: .NET CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
        
    - name: Restore dependencies
      run: dotnet restore 05SOA/RestaurantReviews
      
    - name: Build
      run: dotnet build --no-restore 05SOA/RestaurantReviews
      
    - name: Test
      run: dotnet test --no-build --verbosity normal 05SOA/RestaurantReviews
